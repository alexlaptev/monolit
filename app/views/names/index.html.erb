<h1>Listing names</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Img url</th>
    <th>Size</th>
    <th>Color</th>
    <th>Price</th>
    <th>Sub category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @names.each do |name| %>
  <tr>
    <td><%= name.description %></td>
    <td><%= name.img_url %></td>
    <td><%= name.size %></td>
    <td><%= name.color %></td>
    <td><%= name.price %></td>
    <td><%= name.sub_category_id %></td>
    <td><%= link_to 'Show', name %></td>
    <td><%= link_to 'Edit', edit_name_path(name) %></td>
    <td><%= link_to 'Destroy', name, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Name', new_name_path %>
